describe("test suit name", () => {

    before("before all",()=>{
        cy.log("this will run before all")
    })
    
    beforeEach("before each",()=>{
        cy.log("this will run before all")
 
    })  
    
    aftereEach("after each",()=>{
        cy.log("this will run before all")

})
it("test case 1 name", ()=>{


})
it("test case 2 name", ()=>{


})
t("test case 3 name", ()=>{


})
})
